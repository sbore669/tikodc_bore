import 'package:flutter/material.dart';

class UserInfospage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Boîte de reception')),
    );
  }
}
